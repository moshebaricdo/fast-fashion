/** Outline border shared by toolbar icon buttons, inputs, and segmented controls */
export const toolbarOutlineBorder = "border border-stone/30";

export const toolbarOutlineInput = `${toolbarOutlineBorder} bg-white outline-none focus:border-stone/45`;

/** Popover panels anchored from toolbar controls — matches grid card radius. */
export const toolbarPopoverSurface =
  "rounded-2xl border border-stone/15 bg-white p-4 shadow-[0_8px_32px_rgba(0,0,0,0.1)]";
