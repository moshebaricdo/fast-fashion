import { Icon, type LucideProps } from "lucide-react";
import { trousers } from "@lucide/lab";

/** @lucide/lab — not yet in the main lucide set */
export function Trousers(props: LucideProps) {
  return <Icon iconNode={trousers} {...props} />;
}
